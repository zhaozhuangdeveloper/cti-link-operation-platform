export default {
  state: {
    deploy: {},
    active: {}
  },
  mutations: {
    setDeploy (state, deploy) {
      state.deploy = deploy
    },
    setActive (state, active) {
      state.active = active
    }
  },
  actions: {
  },
  modules: {
  }
}
